<!doctype html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>Blattle of the Blands</title>
    <link rel='stylesheet' href='/style.css'>
  </head>
  <body>
    <% battle.rounds.forEach((round, i) => { %>
      <h2>Round <%= i + 1 %></h2>
      <% round.performances.forEach((perf) => { %>
        <p><%- include('band', { band: perf.band }) %> - <%= perf.song.name %> - <b><%= perf.score %></b></p>
      <% }); %>
      <h3>Eliminated: <%- include('band', { band: round.performances[round.performances.length - 1].band }) %></h3>
    <% }); %>

    <h2>Winner: <%- include('band', { band: battle.bands[0].band }) %></h2>
  </body>
</html>
